# Portrait

Drop the hero portrait here as `anil.png`. The home page loads it from `/images/portrait/anil.png`.

To update the photo, replace the file and commit:

```bash
cp ~/Downloads/anil.png public/images/portrait/anil.png
git add public/images/portrait/anil.png
git commit -m "chore: refresh hero portrait"
```
